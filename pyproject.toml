[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Cancer Data Aggregator"
authors = [{name = "CDA", email = "cda-developers@broadinstitute.org"}]
dynamic = ["version", "description"]
